<%= form_for(@rum) do |f| %>
  <% if @rum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rum.errors.count, "error") %> prohibited this rum from being saved:</h2>

      <ul>
      <% @rum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :price %><br />
    <%= f.text_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
